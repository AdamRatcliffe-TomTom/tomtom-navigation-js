import { makeStyles } from "@fluentui/react";

const useButtonStyles = makeStyles({
  largeButton: {
    height: "48px",
    fontSize: "20px",
    borderRadius: "24px"
  }
});

export default useButtonStyles;
